apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.name }}
  annotations:
    github.com/project-slug: ${{ "madeiramadeirabr/" + values.name }}
    sonarqube.org/project-key: ${{ "madeiramadeirabr/" + values.name }}
    github.com/template: basic-repository
spec:
  type: service
  lifecycle: experimental
  owner: ${{ values.squad }}
